name: contract_driven_platform
version: '1.0.0'
config-version: 2

profile: contract_driven_platform

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  contract_driven_platform:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
